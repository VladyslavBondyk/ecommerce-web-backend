# created application.properties inside config in order to give Spring read main
# application.properties all info and then override only line which r exists here

# creating a jdbc url, using H2 driver, mem - in memory db, db name = test
spring.datasource.url=jdbc:h2:mem:test
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
# below = it will ensure that our auto ddl which is creats DB structure from Hibernate runs first.
spring.jpa.defer-datasource-initialization=true
#
spring.mail.port=3025
spring.mail.username=springboot
spring.mail.password=secret
